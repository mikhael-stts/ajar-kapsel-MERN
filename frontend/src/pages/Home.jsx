import { Typography } from "@mui/material";
import { Container } from "@mui/system";
import React from "react";

const Home = () => {
    return (
        <div>
            <Container maxWidth="xl">
                <Typography variant="h2">Ini halaman home</Typography>
            </Container>
        </div>
    );
};

export default Home;
