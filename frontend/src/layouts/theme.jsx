import { createTheme } from "@mui/material";

/**
 * https://zenoo.github.io/mui-theme-creator/#Accordion
 */
export const themeOptions = createTheme({
    palette: {
        mode: "light",
        primary: {
            main: "#3f51b5",
        },
        secondary: {
            main: "#f50057",
        },
    },
});
